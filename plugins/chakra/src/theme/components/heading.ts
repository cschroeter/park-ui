import { defineStyleConfig } from '@chakra-ui/react'

export const Heading = defineStyleConfig({
  baseStyle: {
    color: 'fg.default',
    fontWeight: 'semibold',
    textStyle: 'xl',
  },
  defaultProps: {},
  sizes: {},
  variants: {},
})
