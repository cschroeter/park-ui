import { Button, ButtonGroup } from '@/components/ui'

export const App = () => {
  return (
    <ButtonGroup variant="outline">
      <Button>Save</Button>
      <Button>Cancel</Button>
    </ButtonGroup>
  )
}
