import type { Meta } from '@storybook/react'

const meta: Meta = {
  title: 'Buttons / Clipboard',
}

export default meta

export { App as basic } from './basic'
export { App as button } from './button'
export { App as input } from './input'
export { App as timeout } from './timeout'
