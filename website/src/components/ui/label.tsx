import { ark } from '@ark-ui/react'
import { styled } from 'styled-system/jsx'
import { label } from 'styled-system/recipes'

export const Label = styled(ark.label, label)
export type LabelProps = typeof Label
